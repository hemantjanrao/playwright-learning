# Copy to .env.prod and set production values.
# Never commit real secrets. Use CI secrets for pipelines.

TEST_ENV=prod
BASE_URL=https://www.example.com
API_BASE_URL=https://api.example.com
E2E_USERNAME=
E2E_PASSWORD=
HEADLESS=true
ALLURE_REPORT=false
