# Copy to .env.dev, .env.qa, .env.staging, or .env.prod (see .env.prod.example) and adjust values.
# Never commit real secrets. Use CI secrets for pipelines.

TEST_ENV=dev
BASE_URL=https://www.saucedemo.com
API_BASE_URL=https://jsonplaceholder.typicode.com
E2E_USERNAME=standard_user
E2E_PASSWORD=secret_sauce
HEADLESS=true
ALLURE_REPORT=false
